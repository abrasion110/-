# README — test_bug.py

تاریخ: 2026-08-23 17:30:54

## مراحل انجام‌شده در این جلسه

1. `echo "prnt('Hello from Termux Agent Test')" > test_bug.py`

## این کد چیکار می‌کند

* این کد یک پیام "Hello from Termux Agent Test" را در صفحه نمایش می دهد
* برای اجرای این کد به محیط برنامه نویسی پایتون نیاز دارید
* باید برنامه پایتون را بر روی دستگاه خود نصب کرده باشید
* این کد نیازی به فایل یا داده های خاصی ندارد
* فقط کافی است کد را کپی کرده و در محیط پایتون اجرا کنید

## کاربر برای اجرا چه کاری باید انجام دهد

1. مطمئن شوید فایل `test_bug.py` در همین پوشه موجود است.
2. دستور زیر را اجرا کنید:
   ```
   python3 test_bug.py
   ```
3. اگر کلید API نیاز دارد، مطمئن شوید `OPENROUTER_API_KEY` در `.bashrc` تنظیم شده باشد.
